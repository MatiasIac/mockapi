These self-signed localhost certificate/key fixtures are public test data, generated
for HTTPS integration tests. Never use this key for a real service. Tests disable
certificate verification only for requests to their temporary local HTTPS server.
